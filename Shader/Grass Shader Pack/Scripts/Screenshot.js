#pragma strict

var shotName : String = "Screenshot";
var shotNumber : int = 0;
var scaling : float = 1;

function Update () 
{
	//if (Input.GetKeyDown (KeyCode.F10)) {
	//	Application.CaptureScreenshot (Application.dataPath + "/" + shotName + shotNumber + ".png", scaling);
	//	shotNumber++;
	//}
}